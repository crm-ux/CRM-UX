<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Assignment Priority: hide star field, show dropdown (same as Lead Info).
        High priority so this applies after Studio/custom views.
    -->
    <record id="crm_lead_form_assignment_priority" model="ir.ui.view">
        <field name="name">crm.lead.form.assignment.priority.dropdown</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="priority">9999</field>
        <field name="arch" type="xml">

            <xpath expr="//sheet//field[@name='priority']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//field[@name='priority'][@widget='priority']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//field[@name='x_assign_to_id']" position="after">
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

            <xpath expr="//group[@string='ASSIGNMENT']//field[@name='priority']" position="replace">
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

            <xpath expr="//group[@string='Assignment']//field[@name='priority']" position="replace">
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

        </field>
    </record>

</odoo>
