<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Hide default Odoo priority (star widget) on the form.
        Show x_priority_selection dropdown (Low / Medium / High) instead.
        The real priority field stays on the record for CRM logic/filters.
    -->
    <record id="crm_lead_form_hide_default_priority" model="ir.ui.view">
        <field name="name">crm.lead.form.hide.default.priority</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="priority">9999</field>
        <field name="arch" type="xml">

            <!-- Hide all default priority fields (stars) on the form -->
            <xpath expr="//form//field[@name='priority']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!-- Assignment: hidden storage field + visible dropdown -->
            <xpath expr="//field[@name='x_assign_to_id']" position="after">
                <field name="priority" invisible="1"/>
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

            <xpath expr="//group[@string='ASSIGNMENT']//field[@name='priority']" position="replace">
                <field name="priority" invisible="1"/>
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

            <xpath expr="//group[@string='Assignment']//field[@name='priority']" position="replace">
                <field name="priority" invisible="1"/>
                <field name="x_priority_selection" string="Priority"/>
            </xpath>

        </field>
    </record>

</odoo>
