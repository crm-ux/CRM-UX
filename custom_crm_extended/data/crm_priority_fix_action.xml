<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_fix_assignment_priority_view" model="ir.actions.server">
        <field name="name">Fix Assignment Priority Dropdown</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="state">code</field>
        <field name="code" type="py">
env['crm.lead.view.patch'].ensure_assignment_priority_dropdown()
env['ir.config_parameter'].sudo().set_param(
    'custom_crm_extended.priority_view_patch_version', '1.2.4')
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': 'Priority views updated',
        'message': 'Assignment Priority is now a dropdown. Refresh the lead form (Ctrl+F5).',
        'type': 'success',
        'sticky': False,
    },
}
        </field>
    </record>

    <record id="menu_fix_assignment_priority" model="ir.ui.menu">
        <field name="name">Fix Assignment Priority UI</field>
        <field name="parent_id" ref="crm.crm_menu_config"/>
        <field name="action" ref="action_fix_assignment_priority_view"/>
        <field name="sequence">500</field>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
    </record>

</odoo>
